extends: default

rules:
  document-start:
    level: ignore
  indentation:
    spaces: consistent
    level: error
  line-length:
    max: 150
    level: warning
  comments:
    level: ignore
  trailing-spaces:
    level: ignore
