apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-endpoint 
  labels:
    app: edge-endpoint
spec:
  template:
    spec: 
      containers:
      - name: edge-endpoint 
        env: 
        - name: DEFAULT_PIPELINE_CONFIG
          # A simple, lightweight, CPU-compatible pipeline
          value: generic-cached-timm-resnet18-knn 




