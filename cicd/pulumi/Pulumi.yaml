name: ee-cicd
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: CI/CD for Edge Endpoint
config:
  #ee-cicd:instanceType: g4dn.xlarge
  # TODO: change back to g4 when this is working better.
  # (I think t3's will turn on/off much faster.)
  ee-cicd:instanceType: t3.xlarge
